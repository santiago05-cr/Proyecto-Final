def test_login_fallido(client):
    response = client.post(
        "/login",
        data={"username": "noexiste", "password": "incorrecto"}
    )

    assert response.status_code == 200
    assert "incorrectos" in response.text.lower()
