import pytest
from app import app  # Ajustar si tu archivo principal no se llama app.py

@pytest.fixture
def client():
    app.config["TESTING"] = True
    with app.test_client() as client:
        yield client